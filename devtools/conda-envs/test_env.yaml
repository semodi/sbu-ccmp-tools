name: test
channels: 
  - base
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - ase
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

